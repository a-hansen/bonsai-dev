# Agent Plan - Phase 4: Creation, Associations, Cleanup, and Validation

**[Meta: Agent-maintained | Active Phase Detail | Compress when done]**
**Project:** `bonsai-dev` | **Parent:** `../agent_plan.md`
**Phase Status:** Complete
**Plan Status:** Complete
**Mode:** Single-pass

## Objective & Scope

**Objective:** Complete the approved workspace refactor by bringing project and map creation onto the current workspace model, adding bounded project-to-map association management, removing stale superseded guidance, and validating the integrated project/map experience in Embedded Bonsai and reusable Bonsai Home modes.
**Inputs:** Approved `.bonsai/specification.md`, `requirements.md`, and `architecture.md`; the active Phase 4 roadmap; completed Phase 1 workspace routing, Phase 2 shared lifecycle, and Phase 3 map-workspace execution; current creation prompts, context and mapping skills, category guides, README examples, and focused workspace validation.
**In Scope:** Rename and rework the Web UI creation prompts as `.bonsai/prompts/create_project.md` and `.bonsai/prompts/create_map.md`; make their initial packages use the canonical startup bootstrap and valid project/map workspace memory; preserve existing-project update and optional human-owned calibration behavior where applicable; add and remove explicit useful-map selections through project `agent_context.md`, where every association identifies a reusable generated map in the active map store and never a repository-local map workspace; reconcile affected standard category guides through the artifact-index workflow; align active README/examples and the generated-map calibration reference; remove stale active references to superseded creation and calibration artifacts; add focused creation/association/reference validation; run the complete workspace regression suite; reconcile project execution memory.
**Out of Scope / Do Not Do Yet:** Changes to approved `.bonsai/specification.md`, project requirements, project architecture, or human-owned map calibration; compatibility aliases for renamed prompts; migration or rewriting of archived project history, unrelated existing project memory, map workspaces, or generated maps; generated-map creation as a side effect of Web UI map creation or project association; automatic dependency-to-map resolution; a map registry, generic workspace type, generic empty-workspace creation, nested map-plan hierarchy, Faber integration, or Bonsai Home layering redesign.
**Expected Deliverables:** `.bonsai/prompts/create_project.md` and `.bonsai/prompts/create_map.md` with the superseded prompt files removed; association behavior in `.bonsai/skills/code_maps.md` and `.bonsai/skills/agent_context.md`; only necessary aligned edits to `.bonsai/prompts/implementation.md` and `.bonsai/templates/code_map_template.md`; reconciled `.bonsai/prompts/prompts.md` and `.bonsai/skills/skills.md` through `.bonsai/skills/artifact_index.md`; current root, standard, and example README guidance; focused `tests/bonsai_creation_associations.py`; passing workspace regression and reference-integrity checks; reconciled roadmap, state, and this phase plan.

## Execution Constraints

- **Implementation Scope:** The active standard's two Web UI creation workflows, project map-association behavior, directly affected routing and generated-map guidance, active category guides and README/examples, focused dependency-free validation, and this project's agent-owned execution memory.
- **Approved Boundaries:** Project and map creation produce repository-root packages under their concrete `.bonsai/projects/<project>/` and `.bonsai/maps/<map>/` locations and include the canonical repository-local bootstrap for initial synthesis. Project creation preserves project-only design and Phase 1 planning semantics. Map creation establishes repository-local workspace memory and optional human-owned calibration but never generated reusable map output. Project map selections live only in project `agent_context.md` and identify reusable generated maps resolved from the active map store; repository-local map-workspace candidates are a separate collection and are never valid association targets merely because they exist or share a name. Changing an association never changes generated maps, map workspaces, workspace identity, project final truth, or roadmap state. Standard artifacts remain Bonsai-Home-relative and category guides remain concise routing aids.
- **Durable Contracts:** None newly established. This phase implements the already approved creation-workflow identities, workspace package shapes, canonical `Useful code maps:` association form, ownership boundaries, and Embedded/reusable-home behavior. It must not introduce a compatibility alias, registry, resolver, schema layer, or new workspace kind.
- **Human Review Focus:** Confirm the single-pass mode; the detailed-plan boundary; the exact creation-prompt rename/removal and package responsibilities; the split between `code_maps.md` entry actions and `agent_context.md` association mutation; the active-document cleanup boundary that preserves archives and intentional final-truth references; and the end-to-end validation needed before declaring the workspace-refactor roadmap exhausted.

## Ordered Work

### Implementation

- **Step 1 — Replace and modernize the creation workflows:** Rename the project and map Web UI prompts to their approved identities; make initial project synthesis include `workspace.md` and the exact canonical current bootstrap while preserving project design/update and Phase 1 planning boundaries; expand map creation from calibration-only packaging to a complete resumable map workspace containing `workspace.md`, `agent_plan.md`, `agent_state.md`, optional `map_calibration.md`, and the canonical bootstrap without generating map output; align the implementation kernel's project-design routing; begin focused creation validation. | **Status:** `Complete` | **Files:** `.bonsai/prompts/create_project_memory.md` (removed), `.bonsai/prompts/create_project.md` (added), `.bonsai/prompts/create_map_calibration.md` (removed), `.bonsai/prompts/create_map.md` (added), `.bonsai/prompts/implementation.md`, `tests/bonsai_creation_associations.py`, `tests/bonsai_map_workspace.py` | **Done:** Both new prompts are complete Web UI artifact-producing workflows with safe names, exact repository-root package shapes, current workspace memory, canonical bootstrap equality, honest readiness, protected ownership, and no old-name aliases; map creation demonstrably emits no reusable map artifacts. Focused creation checks pass 8 cases, and the existing startup, lifecycle, and map-workspace suites pass 19, 14, and 19 cases respectively.
- **Step 2 — Add bounded project-to-map association management:** Expose add/remove association actions through **Manage Code Maps** only when a project context makes them applicable; delegate project-context selection maintenance to `agent_context.md`; validate the concrete project and reusable generated-map identity from the active map store rather than from repository-local map-workspace candidates; preserve unrelated context; use the canonical compact useful-map form; avoid duplicates; remove empty association structure when appropriate; and prove generated maps, map workspaces, and workspace/session state remain unchanged. | **Status:** `Complete` | **Files:** `.bonsai/skills/code_maps.md`, `.bonsai/skills/agent_context.md`, `tests/bonsai_creation_associations.py` | **Done:** Project-only code-map entry actions now select usable generated maps independently from map workspaces and delegate one-file canonical association mutation to agent context. Add, remove, no-op, invalid-project, workspace-only rejection, context preservation, empty-structure cleanup, direct future selection reuse, and generated-map/workspace/execution-memory immutability are covered by 16 passing focused cases; startup, shared-lifecycle, and map-workspace regressions remain green at 19, 14, and 19 cases.
- **Step 3 — Reconcile artifact discovery and active guidance:** Apply the artifact-index workflow to the prompt renames and material skill responsibility changes; align the code-map template's calibration terminology; update current root, standard, self-development, and Task Tracker guidance/examples to the workspace model; remove stale active references to superseded prompt names, `map_repo.md`, obsolete staging paths, and obsolete package shapes while preserving archives and intentional final-truth descriptions of retired names. | **Status:** `Complete` | **Files:** `.bonsai/prompts/prompts.md`, `.bonsai/skills/skills.md`, `.bonsai/templates/code_map_template.md`, `README.md`, `.bonsai/README.md`, `.bonsai/projects/bonsai-dev/README.md`, `.bonsai/projects/task-tracker/README.md`, `tests/bonsai_creation_associations.py` | **Done:** Prompt and skill guides now exactly cover the active same-home artifacts and describe the renamed creation workflows plus current map/context responsibilities. The generated-map template treats `map_calibration.md` as source-local human input rather than generated output. Active root, standard, self-development, and Task Tracker guidance now uses the workspace-native project/map packages, explicit map resume, generated-output separation, and context-only useful-map selections. Four focused guide/reference cases bring the Phase 4 suite to 20 passing tests; scoped stale-reference searches find no retired creation, calibration, or staging names.
- **Step 4 — Validate the integrated model and reconcile completion:** Complete focused fixtures for creation, associations, ownership, reference integrity, and environment behavior covering Embedded Bonsai fallback, an external reusable Bonsai Home, and the supported self-hosting case where `BONSAI_HOME` resolves to the repository-local `.bonsai` directory; run all workspace startup, lifecycle, map-workspace, and Phase 4 suites; validate category-guide coverage and the authorized diff; classify actual final-truth impact; reconcile this plan, roadmap, and state; declare the current body of work complete only if Phase 4 is complete and no approved roadmap work remains. | **Status:** `Complete` | **Files:** `tests/bonsai_workspace_foundation.py`, `.bonsai/start.md`, `.bonsai/prompts/create_project.md`, `.bonsai/prompts/create_map.md`, `README.md`, `.bonsai/projects/bonsai-dev/agent_plan.md`, `.bonsai/projects/bonsai-dev/agent_state.md`, this phase plan | **Done:** Added explicit startup fixtures for Embedded fallback and repository-local self-hosting alongside the existing external reusable-home case; preserved byte-identical canonical bootstrap packaging while removing incidental trailing whitespace; normalized the authorized root README diff; and reran all required suites. The final 74 cases pass (21 startup, 14 lifecycle, 19 map-workspace, 20 creation/association), including category-guide, active-reference, ownership, creation, association, and environment behavior. The authorized diff is whitespace-clean, actual final-truth impact is `None`, Phase 4 is complete, and the approved four-phase roadmap is exhausted.

## Validation & Done Criteria

- **Validation Strategy:** Add a dependency-free Python harness that inspects active Markdown contracts and models project/map package manifests, name validation, bootstrap equality, initial roadmap/state agreement, optional calibration ownership, association add/remove/no-op behavior, rejection of workspace-only association candidates, project and generated-map identity failures, generated-map and map-workspace immutability, category-guide coverage, and path resolution for Embedded fallback, external reusable-home, and repository-local self-hosting `BONSAI_HOME` cases. Run `python3 tests/bonsai_workspace_foundation.py`, `python3 tests/bonsai_workspace_lifecycle.py`, `python3 tests/bonsai_map_workspace.py`, and `python3 tests/bonsai_creation_associations.py`; run focused searches over active standard and guidance paths for superseded names while excluding archives and intentional approved-truth retirement statements; run `git diff --check` on the authorized Phase 4 change set.
- **Architecture / Contract Validation:** Verify both creation prompts package the exact canonical `.bonsai/start.md`; project output contains a valid project `workspace.md` and defers detailed Phase 1 planning; map output contains valid map workspace memory, optional human-owned calibration, and no generated map output; association targets are usable generated maps from the active map store and never repository-local map workspaces; association writes are confined to the selected project's operational context and preserve unrelated context, generated output, map workspaces, execution memory, and session identity; prompt and skill guides remain Bonsai-Home-relative and complete; Embedded fallback, external reusable-home, and repository-local self-hosting `BONSAI_HOME` cases preserve the distinction between repository-local workspace memory and reusable map output.
- **Definition of Done:** The approved `create_project.md` and `create_map.md` workflows replace their superseded predecessors without aliases; project map associations can be safely added and removed through code-map management using only reusable generated maps in the active map store; active guides, examples, templates, and references match the current workspace model; all focused and regression validation passes in Embedded fallback, external reusable-home, and repository-local self-hosting `BONSAI_HOME` scenarios; no unapproved final-truth change is required; Phase 4 is complete and the approved four-phase roadmap is exhausted.

## Context & Wrap-up

- **Dependencies:** Approved workspace-refactor specification and project final truth; completed workspace routing, shared lifecycle, and map-workspace execution; current canonical `.bonsai/start.md`; `skills/artifact_index.md` for required category-guide reconciliation; Python 3 standard library for focused validation.
- **Risks:** The existing creation prompts embed obsolete bootstrap and memory shapes, so partial renames could produce unusable packages. Map creation must preserve the distinction between human calibration, repository-local execution memory, and generated output. Association edits could overwrite unrelated operational context or accidentally couple projects to map lifecycle. Broad documentation cleanup could erase intentional historical/final-truth mentions or overwrite unrelated working-tree edits, so active targets and allowed references must remain explicit. Markdown is executable workflow surface, making exact names, package manifests, and gate wording validation-critical.
- **Open Questions:** None.
- **Completion Summary:** **Outcome:** Step 1 replaced the two superseded Web UI prompts with workspace-native `create_project.md` and `create_map.md`, kept their packaged bootstrap byte-identical to the active canonical anchor, preserved project Phase 1 planning and ownership gates, made map creation resumable without generated output, aligned implementation routing, and established the first 8 focused creation cases. Step 2 added project-only association actions for valid reusable generated maps, delegated canonical one-file selection maintenance to agent context, preserved unrelated context and all generated/workspace/session state, and expanded focused validation to 16 passing cases while keeping the 19 startup, 14 lifecycle, and 19 map-workspace regressions green. Step 3 reconciled prompt/skill discovery against the active artifact inventory, updated calibration terminology and active workspace guidance, removed retired active references, and added four passing guide/reference cases for a 20-case focused suite. Step 4 added explicit Embedded fallback and repository-local self-hosting startup coverage, validated the existing external reusable-home scenario and integrated ownership/environment model, removed incidental whitespace churn from the authorized diff, and completed the final 74-case regression pass. Final-truth impact: `None`. The four-phase workspace-refactor roadmap is exhausted. | **Unlocked:** Body-of-work completion.

## Maintenance Rules

- Treat this file as agent-owned active execution memory, not product or architecture truth.
- Keep `agent_plan.md` roadmap-level; do not duplicate this plan's detailed sequencing there.
- Keep `agent_state.md`, `agent_plan.md`, and this plan aligned for phase, mode, plan status, pass, review state, readiness, blockers, and exact next step.
- Preserve approved project final truth, contracts, and boundaries during execution.
- Do not introduce interfaces, adapters, builders, abstraction layers, dependency constraints, or other structure merely to satisfy Bonsai workflow.
- Follow project conventions and relevant source, developer-context, and agent-context guidance for implementation and testing style.
- If required behavior conflicts with approved final truth or a durable contract, stop for phase-plan correction, final-truth reconciliation, or renewed contract review as applicable.
- Set `Plan Status: Ready for Review` only when drafting is complete and approval is required. Set `Approved` only after explicit human approval.
- Reconcile execution memory whenever a gate or current execution fact changes. Correct a stale or inconsistent plan before substantive execution continues.
- Compress completed detail when it no longer helps resumption; preserve only enough summary to explain the outcome and next capability.
